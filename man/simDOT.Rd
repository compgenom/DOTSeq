% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simDOT}
\alias{simDOT}
\title{Simulate Differential ORF Translation}
\usage{
simDOT(
  ribo,
  rna,
  te_genes = 10,
  bgenes = 10,
  num_samples = 3,
  conditions = 2,
  bcoeff = 0.9,
  num_batches = 1,
  batchScenario = "balanced",
  seed = NULL
)
}
\arguments{
\item{ribo}{A matrix or data frame of ribosome profiling counts (genes x samples).}

\item{rna}{A matrix or data frame of RNA-seq counts (genes x samples).}

\item{te_genes}{Numeric (percentage). Proportion of genes to be assigned as differential translation efficiency (default: 10).}

\item{bgenes}{Numeric (percentage). Proportion of genes to carry a batch effect (default: 10).}

\item{num_samples}{Integer. Number of biological replicates per condition (default: 4).}

\item{conditions}{Integer. Number of experimental conditions (default: 2).}

\item{bcoeff}{Numeric. Magnitude of batch effect coefficient (default: 0.9).}

\item{num_batches}{Integer. Number of batches (default: 1).}

\item{batchScenario}{Character. One of "balanced", "confounded", "random", "unbalanced", "nested", "modalitySpecific".}

\item{seed}{An optional integer. Use to set the seed for reproducible results (default: NULL).}
}
\value{
A list with:
\item{simData}{Combined count matrix (genes x samples)}
\item{colData}{Sample-level metadata}
\item{labels}{Vector indicating true positives (1) and negatives (0)}
}
\description{
This function simulates ribosome profiling and matched RNA-seq count matrices.
}
