% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simDOT}
\alias{simDOT}
\title{Simulate Differentially Translated ORFs}
\usage{
simDOT(
  ribo,
  rna,
  te_genes = 10,
  bgenes = 10,
  num_samples = 4,
  conditions = 2,
  bcoeff = 0.9,
  num_batches = 1
)
}
\arguments{
\item{ribo}{A matrix or data frame of ribosome profiling counts (genes x samples).}

\item{rna}{A matrix or data frame of RNA-seq counts (genes x samples).}

\item{te_genes}{Numeric (percentage). Proportion of genes to be assigned as differential translation efficiency (default: 10).}

\item{bgenes}{Numeric (percentage). Proportion of genes to carry a batch effect (default: 10).}

\item{num_samples}{Integer. Number of biological replicates per condition (default: 4).}

\item{conditions}{Integer. Number of experimental conditions (default: 2).}

\item{bcoeff}{Numeric. Magnitude of batch effect coefficient (default: 0.9).}
}
\value{
A list with two elements:
\itemize{
\item \code{simData} - A combined count matrix (genes x samples),
including both Ribo-seq and RNA-seq simulations.
\item \code{colData} - A data frame of sample-level metadata, including
replicate, strategy ("ribo" or "rna"), and batch assignment.
\item \code{labels} - A numeric vector indicating true positives and true negatives.
}
}
\description{
This function simulates matched ribosome profiling and RNA-seq count matrices.
}
\examples{
# Simulate DOT with default settings
simRes <- simDOT(ribo_counts, rna_counts)
str(simRes$simData)
head(simRes$colData)


}
